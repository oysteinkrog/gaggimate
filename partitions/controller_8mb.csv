# 8 MB flash, controller. Byte-identical to the table in the released
# controller firmware, so a device flashed with this keeps taking OTA
# updates from and to released builds without an erase.
#
# Dual OTA fits easily here: the controller image is ~0.85 MB against
# 3.19 MB slots. Do not collapse this to a single slot — BLE OTA writes
# the *next* slot, and with only one slot IDF hands back the running
# partition and the update erases the app it is running from.
# Name,     Type, SubType, Offset,  Size,    Flags
nvs,        data, nvs,     0x9000,  0x5000,
otadata,    data, ota,     0xe000,  0x2000,
app0,       app,  ota_0,   0x10000, 0x330000,
app1,       app,  ota_1,   0x340000,0x330000,
spiffs,     data, spiffs,  0x670000,0x180000,
coredump,   data, coredump,0x7F0000,0x10000,
